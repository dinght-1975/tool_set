# 执行日志配置示例
# 复制此文件为 .env 并修改相应的配置

# 日志存储类型: mysql, sqlite, file
# 缺省为 file 模式，目录为当前路径下的 execution_logs
LOG_TYPE=file

# MySQL 配置 (当 LOG_TYPE=mysql 时使用)
DB_IP=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=execution_logs

# SQLite 配置 (当 LOG_TYPE=sqlite 时使用)
SQLITE_FILE_PATH=./execution_logs.db

# 文件日志配置 (当 LOG_TYPE=file 时使用)
# 缺省目录为当前路径下的 execution_logs
LOG_FILE_DIR=./execution_logs
